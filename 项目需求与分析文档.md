# 智慧学习平台项目需求与分析文档

## 1. 文档目的

本文档用于明确智慧学习平台（`learning-platform`）的业务目标、用户需求、功能需求、非功能需求、数据需求与实施边界，为后续开发、测试、验收和迭代提供统一依据。

---

## 2. 项目概述

### 2.1 项目背景

当前在线学习产品普遍存在课程推荐不够个性化、学习过程割裂、互动沉淀不足的问题。本项目以“学习+社区+管理”一体化为目标，提供从注册、选课、学习、互动到管理统计的完整闭环。

### 2.2 项目目标

1. 建立稳定可用的在线学习平台基础能力（用户、课程、学习记录）。
2. 提供基于学习偏好/兴趣/目标的课程推荐能力。
3. 提供学习社区互动能力（发帖、评论、点赞、收藏、关注、通知）。
4. 提供管理员运营能力（用户管理、课程管理、公告、统计）。
5. 支撑后续扩展（积分体系、推荐算法增强、运维监控）。

### 2.3 系统范围

**范围内：**
- 用户认证与资料管理
- 课程浏览、筛选、选课、退课、推荐
- 社区帖子/评论/互动
- 私信与站内通知
- 管理员后台与统计

**范围外（当前版本暂不覆盖）：**
- 第三方支付与订单系统
- 完整权限中台（多角色细粒度 RBAC）
- 生产级分布式认证（当前为内存 token）

---

## 3. 干系人与用户角色

## 3.1 干系人
- 产品负责人：定义业务目标与需求优先级
- 开发团队：实现后端、前端、数据库与部署
- 测试团队：功能与回归验证
- 运维/管理员：平台运行、内容与用户治理
- 学习者：课程学习与社区互动核心用户

### 3.2 用户角色定义

1. **普通用户（USER）**
   - 注册登录、维护资料、选课学习、社区互动、私信沟通。
2. **管理员（ADMIN）**
   - 用户冻结/解冻/删除、课程维护、公告发布、平台统计查看。

---

## 4. 业务流程需求

### 4.1 用户注册登录流程
1. 用户输入邮箱并请求验证码。
2. 系统校验发送频率并发送验证码邮件。
3. 用户填写注册信息（含学习偏好、课程兴趣、学习目标）。
4. 系统校验验证码与字段合法性，创建账号。
5. 登录成功后返回 token，后续接口基于 token 鉴权。

### 4.2 学习流程
1. 用户浏览课程列表并按关键词/标签筛选。
2. 查看课程详情后加入课程。
3. 用户在“我的课程”查看已选课程并可退课。
4. 系统基于用户标签与课程标签提供推荐课程。

### 4.3 社区互动流程
1. 用户发布帖子（可含附件）。
2. 其他用户浏览、点赞、收藏、评论。
3. 被互动用户收到通知并可标记已读。
4. 问答类帖子可设置“最佳回答”并标记问题已解决。

### 4.4 管理流程
1. 管理员登录后台。
2. 执行用户治理（查询/冻结/恢复/删除）。
3. 维护课程（新增/修改/删除及封面上传）。
4. 发布系统公告、查看平台统计指标。

---

## 5. 功能需求（详细）

### FR-01 用户认证与账号管理
- 支持邮箱验证码发送与校验。
- 支持用户名/手机号登录。
- 支持管理员邀请码注册管理员账号。
- 支持用户查看个人信息、更新资料、修改密码、上传头像。
- 支持账号状态管理（ACTIVE/FROZEN），冻结账号禁止登录。

### FR-02 课程中心
- 支持课程分页列表、关键词检索、偏好/兴趣/目标筛选。
- 支持课程详情查看。
- 支持加入课程、退选课程。
- 支持“我的课程”查询。
- 支持推荐课程（基于用户标签与课程标签匹配）。

### FR-03 社区中心
- 支持帖子发布、编辑、删除、列表与详情查看。
- 支持帖子点赞/取消点赞、收藏/取消收藏、热门帖子查询。
- 支持评论发布、编辑、删除、点赞、回复。
- 支持问答帖设置最佳回答。
- 支持用户关注/取关。

### FR-04 消息与通知
- 支持用户间私信发送、会话列表、会话详情。
- 支持未读消息统计与已读标记。
- 支持社区行为通知（点赞、评论、回复、关注、最佳回答）。
- 支持公告通知（管理员面向全体用户推送）。

### FR-05 管理后台
- 支持用户分页检索与详情查看。
- 支持用户冻结、解冻、删除。
- 支持课程增删改查与封面上传。
- 支持平台统计（用户、课程、选课、帖子、评论等）。
- 支持全员公告发布。

### FR-06 文件上传
- 支持头像、课程封面、社区附件上传。
- 支持文件大小与格式校验。
- 上传后返回可访问 URL 并用于业务实体保存。

---

## 6. 非功能需求

### NFR-01 安全性
- 所有需要登录的业务接口必须校验 token。
- 管理接口必须校验管理员角色。
- 上传文件需做类型、大小限制。
- 敏感配置（邮箱密码、数据库密码、邀请码）需环境变量覆盖。

### NFR-02 可用性
- 常见错误返回明确提示（参数错误、权限不足、资源不存在等）。
- 支持跨域访问，满足前后端联调需求。
- 数据库初始化失败时不直接阻断应用启动（便于开发环境容错）。

### NFR-03 性能
- 列表接口支持分页，避免大数据量全量返回。
- 高频查询字段需索引支持（用户ID、课程ID、消息读状态、时间等）。
- 推荐与统计在当前版本以可用优先，后续可优化为聚合查询或异步计算。

### NFR-04 可维护性
- 采用分层架构（Controller/Service/Mapper/Entity）。
- 统一采用 REST 风格接口。
- SQL 映射集中在 MyBatis Mapper XML，便于维护与排查。

---

## 7. 数据需求分析

### 7.1 核心实体
- 用户（user）
- 课程（course）
- 选课关系（user_course）
- 帖子（post）
- 评论（comment）
- 帖子点赞（post_like）
- 评论点赞（comment_like）
- 收藏（favorite）
- 关注（follow）
- 通知（notification）
- 私信（message）

### 7.2 关键数据约束
- 用户名/手机号/邮箱唯一。
- 同一用户对同一课程只能选课一次（唯一键）。
- 同一用户对同一帖子/评论点赞唯一、收藏唯一、关注唯一。
- 用户状态区分 ACTIVE/FROZEN。

### 7.3 数据初始化要求
- 系统启动自动初始化数据库与表结构。
- 需提供基础课程及社区示例数据用于冷启动展示。

---

## 8. 接口需求概览

按模块划分接口前缀：
- 认证：`/api/auth`
- 用户：`/api/users`
- 课程：`/api/courses`
- 社区：`/api/community`
- 私信：`/api/messages`
- 统计：`/api/statistics`
- 管理：`/api/admin`
- 图片上传：`/api/upload/image`

接口统一要求：
- 需要登录的接口通过 `Authorization` 头传递 token。
- 返回结构应包含明确的成功/失败语义与错误信息。

---

## 9. 技术与部署需求

### 9.1 技术栈
- Java 17
- Spring Boot 3.5.x
- MyBatis 3.0.x
- MySQL 8.x
- 前端：HTML/CSS/JavaScript（静态资源）
- 构建：Maven

### 9.2 部署要求
- 支持通过环境变量覆盖端口、数据库、邮件、管理员邀请码等配置。
- 上传目录需具备读写权限（头像、课程封面、社区附件）。
- 需保证 `uploads` 静态资源映射可访问。

---

## 10. 约束与风险分析

### 10.1 业务约束
- 管理员账号注册需邀请码。
- 冻结用户不能登录。
- 删除课程时需联动处理已选课用户通知与选课记录清理。

### 10.2 技术风险
1. **认证机制风险**：当前 token 内存存储，重启失效，不适合多实例生产。
2. **统计性能风险**：部分统计通过应用层遍历计算，数据量大时可能性能下降。
3. **文件安全风险**：上传文件需持续加强 MIME 校验和恶意文件防护。
4. **数据库依赖风险**：测试/启动对本地 MySQL 可用性存在依赖。

### 10.3 优化建议
- 认证升级为 JWT + 刷新机制。
- 统计改为 SQL 聚合/离线任务。
- 上传增加统一安全扫描策略。
- 引入缓存与限流机制提高稳定性。

---

## 11. 验收标准

### 11.1 功能验收
- 注册、登录、鉴权、个人信息管理可完整闭环。
- 课程浏览、筛选、详情、选退课、推荐流程可用。
- 社区发帖评论、点赞收藏、关注、通知可用。
- 私信会话与未读统计准确。
- 管理端用户治理、课程管理、公告与统计可用。

### 11.2 质量验收
- 关键接口异常处理清晰，前端可感知。
- 上传文件的大小/类型校验有效。
- 数据唯一性约束生效。
- 基础测试可执行并有可追踪结果。

---

## 12. 版本规划建议

### V1（当前）
- 完成学习平台基础闭环与管理能力。

### V2
- 升级认证体系（JWT）、增强推荐策略、优化统计性能。

### V3
- 引入学习任务、积分成长体系、移动端与多端协同能力。

---

## 13. 附录

### 13.1 术语
- 学习偏好：用户偏好的学习方式（如视觉学习、动手实践）。
- 课程兴趣：用户关注的课程方向（如前端开发、数据科学）。
- 学习目标：用户学习动机（如职业提升、技能拓展）。

### 13.2 参考依据
- 后端接口与服务实现：`src/main/java/com/nchu/learningplatform`
- 数据库脚本：`src/main/resources/sql/schema-learning-platform.sql`
- 系统说明文档：`/home/runner/work/learning-platform/learning-platform/系统使用说明书.md`
