<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.nchu.learningplatform.mapper.FollowMapper">

    <insert id="insert">
        INSERT INTO follow (follower_id, following_id)
        VALUES (#{followerId}, #{followingId})
    </insert>

    <select id="findByFollowerAndFollowing" resultType="com.nchu.learningplatform.entity.Follow">
        SELECT * FROM follow
        WHERE follower_id = #{followerId} AND following_id = #{followingId}
    </select>

    <delete id="delete">
        DELETE FROM follow
        WHERE follower_id = #{followerId} AND following_id = #{followingId}
    </delete>

    <select id="findFollowingIds" resultType="long">
        SELECT following_id FROM follow
        WHERE follower_id = #{followerId}
    </select>

    <select id="findFollowerIds" resultType="long">
        SELECT follower_id FROM follow
        WHERE following_id = #{followingId}
    </select>

    <select id="countFollowing" resultType="int">
        SELECT COUNT(*) FROM follow
        WHERE follower_id = #{userId}
    </select>

    <select id="countFollowers" resultType="int">
        SELECT COUNT(*) FROM follow
        WHERE following_id = #{userId}
    </select>

</mapper>

