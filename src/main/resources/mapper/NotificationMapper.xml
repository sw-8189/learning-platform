<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.nchu.learningplatform.mapper.NotificationMapper">

    <resultMap id="NotificationResultMap" type="com.nchu.learningplatform.entity.Notification">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="type" property="type"/>
        <result column="related_id" property="relatedId"/>
        <result column="from_user_id" property="fromUserId"/>
        <result column="content" property="content"/>
        <result column="is_read" property="isRead"/>
        <result column="create_time" property="createTime"/>
        <result column="from_username" property="fromUsername"/>
        <result column="from_avatar_url" property="fromAvatarUrl"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO notification (user_id, type, related_id, from_user_id, content)
        VALUES (#{userId}, #{type}, #{relatedId}, #{fromUserId}, #{content})
    </insert>

    <select id="findByUserId" resultMap="NotificationResultMap">
        SELECT n.*, u.username as from_username, u.avatar_url as from_avatar_url
        FROM notification n
        LEFT JOIN user u ON n.from_user_id = u.id
        WHERE n.user_id = #{userId}
        ORDER BY n.create_time DESC
        <if test="limit != null">
            LIMIT #{limit}
        </if>
    </select>

    <select id="countUnread" resultType="int">
        SELECT COUNT(*) FROM notification
        WHERE user_id = #{userId} AND is_read = 0
    </select>

    <update id="markAsRead">
        UPDATE notification SET is_read = 1 WHERE id = #{id}
    </update>

    <update id="markAllAsRead">
        UPDATE notification SET is_read = 1 WHERE user_id = #{userId} AND is_read = 0
    </update>

</mapper>

